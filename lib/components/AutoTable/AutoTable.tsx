import {
  TableContainer,
  TableContainerParams,
} from "@lib/components/AutoTable/TableContainer/TableContainer";

export const AutoTable: React.FC<TableContainerParams> = (props) => {
  return <TableContainer {...props}></TableContainer>;
};
